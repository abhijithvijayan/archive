image: node:latest

stages:
  - installation
  - build
  - deploy

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: installation
  script:
    - npm install
  artifacts:
    paths:
      - node_modules/

Build:
  stage: build
  artifacts:
    paths:
      - dist/
  script:
    - npm run build
    - ls
  only:
    - master

pages:
  stage: deploy
  script:
    - mkdir .public
    - mv /dist/* public
  artifacts:
    paths:
      - public
  only:
    - master